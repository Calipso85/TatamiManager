import com.mycompany.tatamimanager.Profesores.*;

import static org.junit.jupiter.api.Assertions.*;
import org.junit.jupiter.api.BeforeEach;
import org.junit.jupiter.api.Test;

public class TestAddProfesores {
/*
    private AddProfesores panel;

    private void new_profesor(){
        panel.getTxt_().setText("Profesor Ejemplo");
    }

    @BeforeEach
    public void setUp() {
        // Inicializa el panel Profesor
        panel = new AddProfesores();
    }

    @Test
    public void test_addProfesor() {
        assertNotNull(panel); // se puede borrar

        // Verifica que los componentes están correctamente inicializados
        assertNotNull(panel.getTxt_());
        assertNotNull(panel.getjLabel1());
        assertNotNull(panel.getjLabel2());
    }

    @Test
    public void test_initComponents() {
        // Verifica que los componentes inicializan por defecto
        assertEquals("", panel.getTxt_().getText());
        assertEquals("jLabel1", panel.getjLabel1().getText());
        assertEquals("jLabel2", panel.getjLabel2().getText());
    }

    @Test
    public void test_btn_GuardarProfesorActionPerformed() {
        new_profesor();
        // No hay metodo aun
        //doClick();
        //assertEquals("Profesor Ejemplo",);
    }

    @Test
    public void test_labelVisibility() { // Quizás borrar
        // Verifica que las etiquetas sean visibles en la interfaz de usuario
        assertTrue(panel.getjLabel1().isVisible());
        assertTrue(panel.getjLabel2().isVisible());
    }
*/
}
